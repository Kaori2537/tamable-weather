# Weather Forecast Dashboard

リアルタイム天気予報ダッシュボード - Open-Meteo APIを使用したSPA

## 使用技術・言語
- Next.js 15.5.4 (App Router)
- React 19 / TypeScript
- Tailwind CSS v4 + カスタムCSS変数
- SWR
- Recharts
- date-fns

## 成果物概要
- あらかじめ用意した主要都市（東京・大阪・札幌・福岡・那覇など）から選択し、Open-Meteoの時系列天気予報を取得
- 48時間（hourly）と7日間（daily）の期間トグル、指標（気温・体感温度・降水量・風速）と摂氏/華氏の切り替えが可能な単一ページのダッシュボード
- Rechartsによる折れ線チャート描画（グラデーション塗り、カスタムツールチップ、レスポンシブ対応）
- 読み込み・エラー時のUIを備え、過剰な再フェッチをSWRのキャッシュで抑制

## 制作背景・動機
長期インターン選考課題として、企業から指定された要件（都市と指標を選択して時系列データを可視化するSPA）に沿って実装。

## 起動手順

1. プロジェクトディレクトリに移動
```bash
cd tamable-weather
```

2. 依存関係のインストール
```bash
npm install
```

3. 開発サーバーの起動
```bash
npm run dev
```

4. ブラウザで http://localhost:3000 にアクセス

## 使用技術、言語

### フレームワーク: Next.js 15.5.4
- **理由**: App Routerによる最新のReact開発体験、SSR対応、パフォーマンス最適化、プロダクション向けの機能が豊富
- **使いこなし**: コンポーネントベースの設計、動的インポート、メタデータ管理、ビルド最適化
- **企業採用実績**: Vercel、Netflix、TikTok、Huluなど大手企業が採用
- **開発現場の現実**: 多くの企業が「React + Next.js」を標準スタックとして採用し、React単体で開発することは少なくなっている

## 工夫点

### 1. コンポーネント設計
- **責務分離**: 各コンポーネントが単一の責任を持つ
- **再利用性**: Props型定義による型安全なインターフェース
- **状態管理**: ローカル状態とグローバル状態の適切な分離

### 2. パフォーマンス最適化
- **メモ化**: useMemo, useCallbackによる不要な再計算防止
- **キャッシュ**: SWRによるAPIレスポンスキャッシュ
- **レート制限対応**: 重複リクエスト防止、エラー時のリトライ制限

### 3. UX/UI改善
- **レスポンシブ**: モバイル・タブレット・デスクトップ対応
- **アクセシビリティ**: ARIA属性、キーボードナビゲーション
- **エラーハンドリング**: 適切なエラーメッセージと回復オプション

### 4. データ管理
- **型安全性**: TypeScriptによる実行時エラー防止
- **永続化**: localStorageによるユーザー設定保持
- **位置情報**: 座標から県名の自動判定
  - 現在地から都市を自動追加、LocalStorageで永続化、削除機能付き

## ディレクトリ構成

```
tamable-weather/
├── app/
│   ├── page.tsx          # メインページ（状態管理・API呼び出し）
│   ├── layout.tsx        # レイアウトコンポーネント
│   └── globals.css       # グローバルスタイル
├── components/
│   ├── BackgroundBlobs.tsx  # 背景アニメーション
│   ├── CitySelect.tsx       # 都市選択コンポーネント
│   ├── MetricSelect.tsx     # 指標選択コンポーネント
│   ├── PeriodToggle.tsx     # 期間切り替えコンポーネント
│   ├── UnitToggle.tsx       # 単位切り替えコンポーネント
│   └── WeatherChart.tsx     # チャート表示コンポーネント
├── lib/
     ├── cities.ts         # 都市データ・型定義
     ├── openMeteo.ts      # API URL構築
     └── units.ts          # 単位変換ロジック
